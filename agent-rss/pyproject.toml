[project]
name = "rss"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "feedparser>=6.0.12",
    "anthropic>=0.40.0",
    "httpx>=0.27.0",
]

[tool.uv]
package = true

[tool.setuptools]
py-modules = ["agent_v1"]

[project.scripts]
agent-rss = "agent_v1:main"
